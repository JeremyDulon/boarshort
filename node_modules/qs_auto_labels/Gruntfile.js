module.exports = function (grunt) {
	grunt.initConfig({
		pkg: grunt.file.readJSON('package.json'),

        auto_labels: {
          project: {
            src: ['tpl/*.html'],
            options: {
              config: 'config/config.js',
              locales: 'locales/*',
              output: 'html',
              base: 'tpl'
            }
          }
        }
	});

    grunt.registerTask('default', ['auto_labels']);
};
