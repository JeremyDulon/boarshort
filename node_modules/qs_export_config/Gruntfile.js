
module.exports = function (grunt) {
	grunt.initConfig({
		pkg: grunt.file.readJSON('package.json'),

        export_config: {
          project: {
            options: {
              config: 'config/config.js',
              output: 'build/config_export'
            }
          }
        }
	});

    grunt.registerTask('default', ['export_config']);
};
