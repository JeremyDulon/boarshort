
module.exports = function (grunt) {
	grunt.initConfig({
		pkg: grunt.file.readJSON('package.json'),

        export_labels: {
          project: {
            options: {
              locales: 'locales/*',
              output: 'build/locales_export',
            }
          }
        }
	});

    grunt.registerTask('default', ['export_labels']);
};
